<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
<!-- Search, should appear as action button -->
<item android:id="@+id/btn_nav_menu"
    android:icon="@drawable/btn_nav_menu"
    android:showAsAction="ifRoom"
    android:title="@string/btn_nav_menu"
    />
<item android:id="@+id/btn_nav_search"
    android:icon="@drawable/btn_nav_search"
    android:showAsAction="ifRoom"
    android:title="@string/btn_nav_search"
    android:orderInCategory="2"/>


</menu>